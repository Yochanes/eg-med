<div class="col-lg-5 col-md-3 col-sm-12 col-xs-12 onspro">
  <div class="row rless">
<!-- <h1 class="heading">{{onsale_title}}</h1> -->
  <div id="onsale" class="owl-theme owl-carousel">
 {% for product in products %}
  <div class="product-layout col-xs-12 cless">
    <div class="product-thumb transition">
      <div class="image"><a href="{{ product.href }}"><img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-responsive center-block" /></a>
          <!-- Webiarch Images Start -->
                 {% set t = 0 %}
                  {% for more_image in product.more_images %}
                  {% for pop in more_image %}
                  {% if t == 0 %}
                    <a href="{{ product.href }}"><img src="{{ pop.popup_more }}" class="img-responsive second-img" alt="hover image"/></a>
                  {% set t = t + 1 %}
                    {% endif %}

                  {% endfor %}
                  {% endfor %}
          <!-- End -->
          {% if product.price %}
          {% if product.special %}
               <span class="sale">{{ pro_sale }}</span>
          {% endif %}
      {% endif %}
      <div data-toggle="tooltip" title="Quickview"  class="bquickv"></div>
      </div>
     <div class="caption text-center">
         {% if product.rating %}
          <div class="rating">
            {% for i in 1..5 %}
            {% if product.rating < i %}
            <span class="fa fa-stack">
              <i class="fa fa-star-o fa-stack-2x"></i>
            </span>
            {% else %}
            <span class="fa fa-stack">
              <i class="fa fa-star fa-stack-2x"></i><i class="fa fa-star-o fa-stack-2x"></i>
            </span>
            {% endif %}
          {% endfor %}</div>{% else %}
          <div class="rating">{% for i in 1..5 %}
          <span class="fa fa-stack"><i class="fa fa-star-o fa-stack-2x"></i></span>
          {% endfor %}</div>
        {% endif %}
        <div class="opbtn">
        <h4  class="protitle"><a href="{{ product.href }}">{{ product.name }}</a></h4>
         {% if product.price %}
        <div class="price">
          {% if not product.special %}
          {{ product.price }}
          {% else %}
          <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span>
          {% endif %}
         {#  {% if product.tax %}
          <span class="price-tax">{{ text_tax }} {{ product.tax }}</span>
          {% endif %} #}
        </div>
        {% endif %}
      </div>
       

      <div class="button-group">
        <button type="button" data-toggle="tooltip" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}');"><svg><use xlink:href="#heart" /></svg></button>
        <button type="button"  onclick="cart.add('{{ product.product_id }}');" class="cartb"><svg><use xlink:href="#pcart"></use></svg><span>{{ button_cart }}</span></button>
        <button type="button" data-toggle="tooltip" title="{{ button_compare }}" onclick="compare.add('{{ product.product_id }}');"><svg><use xlink:href="#compare"/></svg></button>
      </div>
      </div>

    </div>
  </div>

  {% endfor %}


</div>
</div>
</div>

</div>
</div>
</div>
<script type="text/javascript">
    (function($){
    $("#onsale").owlCarousel({
    itemsCustom : [
    [0, 1],
    [320, 2],
    [600, 3],
    [768, 3],
    [992, 1],
    [1200, 2],
    [1410, 2]
    ],
      autoPlay: 6000,
      navigationText: ['<i class="fa fa-angle-left" aria-hidden="true"></i>', '<i class="fa fa-angle-right" aria-hidden="true"></i>'],
      navigation : false,
      pagination:false
    });
   }(jQuery));
</script>