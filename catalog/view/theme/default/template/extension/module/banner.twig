<div class="banner-blk">
	<div class="row sumo">
		<div class="se1">
			{% for banner in banners %}
				  <div class="col-md-7 col-lg-7 col-sm-6 col-xs-12 ser-dsc">
				  <span class="title">{{ banner.description }}
		<a href="{{ banner.link }}" class="bnr-btn">shop now<i class="fa fa-long-arrow-right" aria-hidden="true"></i></a>
				  </span>
					</div>
						<div class="col-md-5 col-lg-5 col-sm-6 col-xs-12 beffect">
				  {% if banner.link %}
				  	<a href="{{ banner.link }}">
				  		<img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive" />
				  	</a>
				  {% else %}
				  	<a href="{{ banner.link }}">
				  		<img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive" />
				  	</a>
				  {% endif %}
				  	</div>
			</div>
			  {% endfor %}
			</div>
	</div>
</div>